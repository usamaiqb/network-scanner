version: 2
updates:
  # Keep pinned GitHub Actions up to date (and lets you safely move to SHA pins).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
